/* style.css */
body {
    font-family: Arial, sans-serif;
    text-align: center;
    margin: 0;
    background-color: #FADADD;
    overflow: hidden;
    animation: celebrate-bg 5s infinite alternate;
}

@keyframes celebrate-bg {
    0% { background-color: #FADADD; }
    50% { background-color: #FFC0CB; }
    100% { background-color: #FFB6C1; }
}

#container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100vh;
    position: relative;
    animation: zoom-in 2s ease-in-out;
}

@keyframes zoom-in {
    from { transform: scale(0.8); opacity: 0; }
    to { transform: scale(1); opacity: 1; }
}

#question {
    font-family: 'Sacramento', cursive;
    font-size: 52px;
    animation: bounce 1s infinite alternate, glow-text 2s infinite alternate;
}

@keyframes bounce {
    from { transform: translateY(0px); }
    to { transform: translateY(-10px); }
}

@keyframes glow-text {
    from { text-shadow: 0 0 5px #FF69B4; }
    to { text-shadow: 0 0 15px #FF1493; }
}

#options {
    margin-top: 20px;
}

button {
    padding: 10px 20px;
    margin: 0 10px;
    font-size: 26px;
    font-family: 'Sacramento', cursive;
    background-color: #FB607F;
    color: white;
    border: none;
    cursor: pointer;
    animation: glow 1.5s infinite alternate;
}

@keyframes glow {
    from { box-shadow: 0 0 5px #FF1493; }
    to { box-shadow: 0 0 20px #FF69B4; }
}

.floating-heart {
    position: absolute;
    bottom: -50px;
    color: #FF69B4;
    font-size: 24px;
    animation: float 5s linear infinite;
}

@keyframes float {
    from { transform: translateY(0); opacity: 1; }
    to { transform: translateY(-100vh); opacity: 0; }
}

#celebratory-text {
    font-family: 'Sacramento', cursive;
    font-size: 52px;
    color: #FB607F;
    margin-top: 20px;
    display: none;
}
